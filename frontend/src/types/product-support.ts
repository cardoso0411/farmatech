export type ProductCategoryOption = {
  id: string;
  code: string;
  name: string;
};

export type ProductGroupOption = {
  id: string;
  local: string;
  groupName: string;
  sngpc: string;
  saleOperation: string;
  code: string;
  caution: string;
};
